# KneeAI Backend

FastAPI service for the KneeAI clinical decision-support prototype.

## Quick start

python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
